import React from 'react'
import { storiesOf } from '@storybook/react'
import { action } from '@storybook/addon-actions'

import Overlay from '_atoms/overlay'

storiesOf('Atoms/Overlay', module).add('Overlay', () => <Overlay />)
